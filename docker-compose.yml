services:
  interface-client:
    build: ./services/interface-client
    environment:
      - STT_URL=http://speech-stt:8001
      - ORCHESTRATOR_URL=http://orchestrator:8002
    ports:
      - "8000:8000"
    depends_on:
      - speech-stt
      - orchestrator

  speech-stt:
    build: ./services/speech/stt
    ports:
      - "8001:8001"

  orchestrator:
    build: ./services/orchestrator
    ports:
      - "8002:8002"